бекенд должен отдавать конкретный статус что сущность не онлайн

нужно как-то отображать ошибки, связанные с connect_error на клиенте

убрать запросы к api внутри vue компонентов...

вместо vuex и текущего api переделать в криптоном api

реализовать игровой онлайн мир World, куда могут заходить чайники. модель World содержит ВСЕ списки онлайновых сущностей которые на данный момент онлайн.

реализовать полноценный вход в онлайн игру

спецроль player не имеет прав кроме авторизации. нужна только для механизма entity owning

таблицы на фронтенд отображают текущее состояние именно БАЗЫ ДАННЫХ,
а онлайновый мир имеет как бы своё собственное состояние, которое как бы опережает базу данных
и это онлайновое состояние обязано периодически сохранятся в базу данных

судя по всему мне вообще не нужен синхронизатор. точнее синхронизатор не должен быть каким то отдельным.

и называть отдельно сокет-ио события тоже не нужно. достаточно просто использовать один метод updateById для любых изменений и автоматических синхронизатор будет вызывать нужные функции в зависимости от того какие поля обновились. хотя тоже сомнительно. ведь нужно как-то реагировать на повторные действия. например включил сущность 2 раза подряд и на второй раз нужно отрисовать звук что не сработало повторное нажатие

может ли аккаунт войти в систему два и более раз ?

в каком окружении запускается vitest ?

если от сервера не приходит ответ клиенту, то клиент должен как-то отреагировать

на фронтенде кнопки всегда неактивные, за исключением успешного подключения
если нет прав для действия, то кнопки тоже неактивны должны быть

необходимо реализовать проверку входящей функции ack

сервер падает при неправильном количестве аргументов в ack-функции от клиента

реализовать проверку схемы входящих данных на фронтенде

перенести тайпскрипт конфиг в отдельную папку

сверстать админскую зону

установить nuxt на фронтенде

установить vue-test-utils на фронтенде

возможно реализовать чтоб сервер умел что-то рендерить через ejs

установить dexie на фронтенд

создать PWA манифест

передавать ли типы в di контейнере ?

io router должен отвечать "method not implemented" или "not allowed" в случае если клиент шлёт неправильный event

как вообщем то и rest router должен так же реагировать на несуществующие методы

сделать нормальную анимацию при удалении tr из таблицы

фронтенд должен стартовать, даже если бекенд не работает

телефон необходимо проверять в соответствии со страной

сделать чтоб бабель удалял комментарии из кода

лучше сделать чтоб при редактировании полей формы или ячейки таблицы при нажатии кнопки save или submit поле становилось бы disable, пока приходит ответ от сервера
ПЛЮС К ЭТОМУ ЖЕ
нужно сделать чтоб кнопка save отображала спиннер во время ожидания ответа от сервера. эти 2 пункта сделают интерфейс более отзывчивым и логичным

когда сущность переходит в режим редактирования. необходимо запретить на фронтенде редактировать эту же сущность во всех остальных её отображениях. иначе будут постоянные проблемы с тем, кто первый обновил. может даже и не только на фронтенде. бекенд может тоже запретить редактировать сущность, пока её редактирует кто-то другой.

порядок полей должен быть такой, который установлен в объекте fields в умном компоненте сущности, а не в том порядке, как с сервера приходит

проработать удаление и редактирование сущности на фронтенде, если отсутствует ресурс на бекенде

если данные пришли с сервера, то у них по определению не может быть ошибки.
данные, которые приходят с сервера всегда корректные.

err это локальное состояние компонента с пользовательским вводом, который сам же и является причиной этой ошибки
не нужно пихать err в состояние сущности и уж темболее err не месть в глобальном состоянии сторы

при разрыве связи с сервером (при перезагрузки) клиент должен обновить все данные. а еще лучше вывести в клиенте сообщение о том, что сервер упал. и остановить работу приложения.

при клике на кнопку delete на фронтенде если бекенд возвращает 404 или 204 статусы, то нужно ли сносить сущность на фронтенде ??
короче бекенд при 404 статусе должен возвращать какой-то вменяемый статус, о том что реально сущности нет в базе данных. а то не понятно, это ошибка сети или реально сущность отсутствует в базе данных.

при включении (либо перезагрузке) и повторном подключении клиентов необходимо разослать всем подключенным клиентам актуальное состояние сервера
ЛИБО
вообще при падении сервера (таймаут 15 секунд) все клиенты должны быть принудительно отключены

если клиент разорвал соединение, необходимо кикнуть его с сервера и потребовать повторную аутентификацию при новой попытке входа

ПРИЧИНЫ РАЗДЕЛЕНИЯ НА bc-cl: и ack?.()
через ack можно отдать одни данные, а через bc-cl: другие
например можно дать инициатору события какие-то данные про его инвентарь и координаты, а бродкастить публично только координаты.
пока што никаких других причин не вижу

необходимо создать файл реэкспортов вендорных типов
type TResponse = ReturnType<typeof Express.Response>
либо отдельные файлы типов для каждого вендора
import {TResponse} from 'TExpress'

состояние - это данные, которые меняются во времени
ongoing(продолжающийся) - это информация о том, что сущность делает в данный момент времени

переделать серверную логику так, чтоб было только состояние, ongoings, и timestamps

оркестрация Kubernetes K8s

при создании сервера нужно установить это:
const io = new Server({serveClient: false})
потому что сервер хостит это: http://localhost:7704/socket.io/socket.io.js

??? при rest запросах сервис должен генерировать <BC-SV> событие и все клиенты обновляют данные на это событие. в отличие от <BC-CL> когда обнавляют все кроме клиента, который вещает.

нужно настроить typescript на понимание алиаса @/ в импортах

должны ли все сущности быть уникальными на всех ресурсах ? 409 статус при добавлении и обновлении ресурса должен быть на всех контроллерах ?

в socket-io разделять id и data в update запросах и ответа довольно геморно.
по-моему гораздо лучше присылать целиком dto и обновлять его в зависимости от сигнатуры метода. если это updateById то обновлять по data.id

да и вообще это разделение на id и data - один сплошной гемор, который вынужденно приходится поддерживать из-за rest http протокола

валидация всех входных данных:
cookies
params
query
body

каждое поле имеет свою проверку
verifyField(fieldName, fieldData)

для каждого входа своя функция
verifyCookies
verifyParams
verifyQuery
verifyBody

все 4 функции можно будет скомпилировать в одну единую мидлварю
verifyInput

нужно разделить мидлвари на 2 вида
shared middlewares (ID, SESSID)
entity middlewares (ADD, UPD)

auth0/google

контейнеризация Docker

реализовать обработку ошибок и исключений
логирование ошибок винстоном
логирование действий винстоном
реализовать логирование бекенда через winston (уже реализовано в битстрейнере)

нужно как-то синхронизировать маршруты роутера со статикой, которую раздаёт express
а лучше сразу с nginx`ом синхронизировать маршруты

фронтенд должен гарантированно подписываться на бродкаст сущности только 1 раз
любая возможность двойной подписки на сущность должна быть запрещена
подписка на бродкасты при mutation.type === 'persons/SET_PERSONS' плохая идея, потому что пользователь имеет кнопки для принудительного обновления(хотя зачем)
можно сделать отписку перед каждой подпиской

рефакторинг псевдотаблиц и Orm в postgres и Type ORM

прописать миграции базы данных

рефакторинг di контейнера в inversify-js

    // teapotService.on('teapot!turned_on', (t: Teapot) =>
    //   this.io?.emit('bc-sv:teapot-turned_on', t),
    // )
    // teapotService.on('teapot!already_turned_on', (t: Teapot) =>
    //   this.io?.emit('bc-sv:teapot-already_turned_on', t),
    // )

    // if (teapot.turnOn()) this.emit('teapot!turned_on', teapot)
    // else this.emit('teapot!already_turned_on', teapot)

    // if (teapot.turnOff()) this.emit('teapot!turned_off', teapot)
    // else this.emit('teapot!already_turned_off', teapot)

    // if (teapot.turnDrain()) this.emit('teapot!turned_drain', teapot)
    // else this.emit('teapot!already_turned_drain', teapot)

    "typeRoots": ["../../node_modules/@types", "../../node_modules", "./types"],
    "types": ["vitest/globals", "node"],

если таблица в базе данных обновилась по rest, то нужно разослать bc-sv сообщение об этом обновлении

на фронтенде при тестовом переключении сессий создаётся множество дублированных соединений socket-io. нужно сделать так чтоб открытым был всегда только 1 сокет
